extends ../layout

block title
	title УСЛУГИ

block content_style
	link(rel='stylesheet', type='text/css', href='/styles/static.css' + version)

block content_scripts
	script(type='text/javascript', src='/scripts/static.js' + version)

block content
	.content_title_block Услуги
	.select_block
		.select_item рекламная открытка
		.select_item фирменный стиль
		.select_item логотип
		.select_item визитки
		.select_item листовка
		.select_item флаер
		.select_item билет
		.select_item буклет
		.select_item листовка
		.select_item флаер
		.select_item билет
		.select_item буклет
	.service_block
		.image
		.about
		.terms
		.price